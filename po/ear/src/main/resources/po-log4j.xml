<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE log4j:configuration SYSTEM "log4j.dtd">

<!-- ===================================================================== -->
<!--                                                                       -->
<!--  Log4j Configuration                                                  -->
<!--                                                                       -->
<!-- ===================================================================== -->

<!-- $Id: log4j.xml 56612 2006-09-07 15:12:39Z thomas.diesler@jboss.com $ -->

<!--
   | For more configuration infromation and examples see the Jakarta Log4j
   | owebsite: http://jakarta.apache.org/log4j
 -->

<log4j:configuration xmlns:log4j="http://jakarta.apache.org/log4j/" debug="false">

   <!-- ================================= -->
   <!-- Preserve messages in a local file -->
   <!-- ================================= -->

   <!-- A time/date based rolling appender -->
    <appender name="POFILE" class="org.jboss.logging.appender.DailyRollingFileAppender">
        <errorHandler class="org.jboss.logging.util.OnlyOnceErrorHandler"/>
        <param name="File" value="${jboss.server.log.dir}/po.log"/>
        <param name="Append" value="false"/>

      <!-- Rollover at midnight each day -->
        <param name="DatePattern" value="'.'yyyy-MM-dd"/>

      <!-- Rollover at the top of each hour
      <param name="DatePattern" value="'.'yyyy-MM-dd-HH"/>
      -->

        <layout class="org.apache.log4j.PatternLayout">
         <!-- The default pattern: Date Priority [Category] Message\n -->
            <param name="ConversionPattern" value="%d{ISO8601} %-5p [%F:%L] : %m%n"/>

         <!-- The full pattern: Date MS Priority [Category] (Thread:NDC) Message\n
         <param name="ConversionPattern" value="%d %-5r %-5p [%c] (%t:%x) %m%n"/>
          -->
            <param name="LocationInfo" value="true"/>
        </layout>
    </appender>

   <!--                          -->
   <!-- Declare the SMTPAppender -->
   <!--                          -->
    <appender name="EMAIL" class="org.apache.log4j.net.SMTPAppender">
        <param name="BufferSize" value="1024" />
        <param name="SMTPHost" value="${log.smtp.host}" />
        <param name="From" value="${log.smtp.from}" />
        <param name="To" value="${log.smtp.from}" />
        <param name="Subject" value="CTEP message processing error" />
        <layout class="org.apache.log4j.PatternLayout">
            <param name="ConversionPattern"
              value="[%d{ISO8601}]%n%n%-5p%n%n%c%n%n%m%n%n" />
        </layout>
        <filter class="org.apache.log4j.varia.LevelRangeFilter">
            <param name="LevelMin" value="ERROR"/>
            <param name="LevelMax" value="FATAL"/>
        </filter>
    </appender>

   <!-- ================ -->
   <!-- Limit categories -->
   <!-- ================ -->

   <!-- Limit the org.apache category to INFO as its DEBUG is verbose -->
   <category name="gov.nih.nci.po">
      <priority value="${log.level.po}"/>
   </category>
   <category name="com.fiveamsolutions.nci.commons">
      <priority value="${log.level.nci-commons}"/>
   </category>
   <category name="EMAIL_ERROR_LOGGER">
      <priority value="ERROR"/>
      <appender-ref ref="EMAIL" />
   </category>


   <!-- ======================= -->
   <!-- Setup the Root category -->
   <!-- ======================= -->
    <root>
		<!--
			The default logging level, for any logger not configured above. Warning:
			setting this too fine-grained can have major performance consequences
		-->
        <priority value="${log.level.default}" />
        <appender-ref ref="POFILE" />
    </root>

</log4j:configuration>
