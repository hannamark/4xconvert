########################### APPLICATION SETTINGS #########################
application.base.path=${user.home}/apps/pa
doc.upload.filepath.relative.dir=doc
pdq.upload.filepath.relative.dir=pdq
batch.upload.filepath.relative.dir=batch_docs
tooltips.filepath.relative.dir=tooltips
pa.server.name=localhost
cteprss.user=/O=caBIG/OU=caGrid/OU=Training/OU=Dorian/CN=ctep-rss
pasystem.user=/O=caBIG/OU=caGrid/OU=Training/OU=Dorian/CN=pa-system
pasystem.password=Coppa#12345
showErrorStackTraces=false

########################### INCLUDE/EXCLUDE VIEWER #########################
#exclude.viewer=true

########################### GENERAL SETTINGS #########################
jboss.deploy.directory=${application.base.path}/paear/jboss-4.0.5.GA-jems-ejb3/server/paear/deploy
jboss.server.jndi.port=31499
jboss.port=39480
selenium.server.port=4444

db.type=postgresql
db.server=localhost
db.name=ctods_pa
db.port=5432
jdbc.driver=org.postgresql.Driver
hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
db.username=pauser
db.password=pauser
jdbc.base=jdbc:${db.type}://${db.server}:${db.port}
jdbc.url=${jdbc.base}/${db.name}

ci.schema.files=csmCreateSchema.sql,csmBootstrapData.sql,PG_CTODS_PA.sql,country.sql,regulatoryAuthority.sql,FUNDING_MECHANISM.SQL,PA_PROPERTIES.SQL,organizationBootstrapData.sql

doc.upload.filepath.loc=${application.base.path}/${doc.upload.filepath.relative.dir}
pdq.upload.filepath.loc=${doc.upload.filepath.loc}/${pdq.upload.filepath.relative.dir}
tooltips.filepath.loc=${application.base.path}/${tooltips.filepath.relative.dir}
po.server.name=localhost
po.port.number=31099
po.username=ejbclient
po.pass=pass
#Pre-shared JNDI principal & credential for pa-grid <-> pa.ear authentication
gridServicePrincipal=Gr1DU5er
gridServiceCredential=ltHZmZ1rqYq8j2uyHEABIQ==

# Registry batch upload docs location
batch.upload.filepath.loc=${application.base.path}/${batch.upload.filepath.relative.dir}

# Messaging properties

# Po Server name to look up
po.topic.serverName=localhost
# Po Server port number for the look up
po.topic.port=31099
# The user name for the secured topic 'topic/POTopic', this user name will be provisioned by PO team, they will creating the CSM user and providing the principla/credentials to log in.
po.topic.userName=subscriber
# The password for the secured user
po.topic.password=pass
# The client id in this case PA so PAApplication
po.topic.clientId=PAApplication

# CSM group for Trial Submitters
# Note: The value has to be in sync with CSM Bootstrap data
# loaded during deployment or created via UPT.
csm.submitter.group=Submitter
csm.remote.group.db.creation.policy = update
csm.remote.group.sync.seconds = 240
#List of comma separated IDPs for which we will allow password editing
idps.allow.password.editing=Dorian

# Use the tier-specific Registration application url
register.mail.body.url=http://${pa.server.name}:${jboss.port}/registry/registerUseractivate.action

gridgrouper.submitter.group=COPPA:PA:Submitter
gridgrouper.admin.user=ctrpgrouper
gridgrouper.admin.password=C0pp4Gr0upP@ss

grid.dorian.url=https\://dorian.training.cagrid.org\:8443/wsrf/services/cagrid/Dorian
gridgrouper.url=https\://grouper.training.cagrid.org\:8443/wsrf/services/cagrid/GridGrouper

# Base URLs for wiki-based help
wikiHelp.baseUrl.pa=https://wiki.nci.nih.gov/x/5IdQAg#NCICTRPProtocolAbstractionHelpTopics-
wikiHelp.baseUrl.registry=https://wiki.nci.nih.gov/x/SYZQAg#NCICTRPRegistrationSiteHelpTopics-
wikiHelp.baseUrl.accrual=https://wiki.nci.nih.gov/x/yALTAg#NCICTRPAccrualHelpTopics-
wikiHelp.baseUrl.viewer=https://wiki.nci.nih.gov/x/xgLTAg#NCICTRPViewerHelpTopics-

maven.username=uploaded
maven.password=password